apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: libreoffice
  annotations:
    kubernetes.io/ingress.class: "traefik"
    traefik.frontend.passHostHeader: "true"
    traefik.backend.loadbalancer.stickey: "true"
spec:
  rules:
  - host: office.leangeder.eu
    http:
      paths:
      - path: /
        backend:
          serviceName: libreoffice
          servicePort: http
